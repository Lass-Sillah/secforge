import type { ClickMultiQuestion } from '../types'

export const HARDENING_SCENARIOS: ClickMultiQuestion[] = [
  {
    id: 'ht-01',
    kind: 'click-multi',
    prompt: 'A Linux web server audit reveals the following running services. Select ALL that should be disabled or replaced.',
    instruction: 'Click every service that is insecure, unnecessary, or should be replaced with a secure alternative.',
    items: [
      { id: 'telnet',  label: 'Telnet — port 23',   detail: 'Sends all data including credentials in plaintext. Replace with SSH.',         flag: true },
      { id: 'ftp',     label: 'FTP — port 21',      detail: 'Credentials and data transferred in cleartext. Replace with SFTP or FTPS.',    flag: true },
      { id: 'ssh',     label: 'SSH — port 22',       detail: 'Encrypted remote access protocol. Industry standard. Should be kept.',         flag: false },
      { id: 'http',    label: 'HTTP — port 80',      detail: 'Unencrypted web traffic. Should redirect to HTTPS (443) only.',                flag: true },
      { id: 'https',   label: 'HTTPS — port 443',    detail: 'Encrypted web traffic via TLS. Required for this web server.',                 flag: false },
      { id: 'smtp',    label: 'SMTP — port 25',      detail: 'Standard mail transfer. Needed for outbound email on a mail server.',          flag: false },
      { id: 'finger',  label: 'Finger — port 79',    detail: 'Reveals user account info. Legacy service. Major privacy/recon risk.',         flag: true },
    ],
    explanation: 'Telnet and FTP transmit credentials in plaintext — trivially intercepted with a packet sniffer. Replace with SSH and SFTP/FTPS. HTTP should be disabled or redirected to HTTPS to prevent downgrade attacks. The Finger service leaks user account info and has no legitimate use today — disable it. SSH, HTTPS, and SMTP are legitimate services for a web/mail server.',
  },
  {
    id: 'ht-02',
    kind: 'click-multi',
    prompt: 'A Windows Server 2019 audit shows the following services and protocols active. Flag ALL that represent hardening failures.',
    instruction: 'Select every misconfiguration, insecure protocol, or unnecessary service.',
    items: [
      { id: 'smb1',    label: 'SMBv1 (Server Message Block v1)',         detail: 'Exploited by EternalBlue (WannaCry). No legitimate modern use. Disable immediately.', flag: true },
      { id: 'smb3',    label: 'SMBv3 (with encryption)',                 detail: 'Modern SMB with AES-128 encryption. Secure. Required for file shares.',               flag: false },
      { id: 'rdp-pub', label: 'RDP (port 3389) exposed to internet',     detail: 'Direct RDP exposure is a prime ransomware entry vector. Must be behind VPN.',         flag: true },
      { id: 'winrm-h', label: 'WinRM over HTTP (port 5985)',             detail: 'Remote management in cleartext. Use HTTPS (port 5986) or restrict to localhost.',      flag: true },
      { id: 'winrm-s', label: 'WinRM over HTTPS (port 5986)',            detail: 'Encrypted remote management. Acceptable when needed with proper ACLs.',                flag: false },
      { id: 'ntp',     label: 'NTP (port 123) — internal client',        detail: 'Time sync is required for Kerberos and logs. Not a hardening issue.',                  flag: false },
      { id: 'llmnr',   label: 'LLMNR / NetBIOS-NS broadcast',           detail: 'Local name resolution with no auth. Trivially exploited by Responder for NTLM relay.', flag: true },
    ],
    explanation: 'SMBv1 enables EternalBlue exploits (WannaCry, NotPetya) — disable it. RDP exposed to the internet is the #1 ransomware initial access vector — put it behind a VPN. WinRM over HTTP sends management traffic in cleartext including credentials — use HTTPS. LLMNR/NetBIOS-NS broadcasts allow Responder attacks to capture NTLM hashes via poisoning — disable both and rely on DNS. SMBv3, WinRM over HTTPS, and NTP are acceptable.',
  },
  {
    id: 'ht-03',
    kind: 'click-multi',
    prompt: 'A network switch\'s management configuration is reviewed. Select ALL settings that represent security misconfigurations.',
    instruction: 'Flag every insecure management protocol or setting.',
    items: [
      { id: 'snmpv1',  label: 'SNMPv1 with community string "public"', detail: 'Community strings are sent in plaintext and "public" is default. Trivially intercepted.', flag: true },
      { id: 'snmpv2',  label: 'SNMPv2c with community string "admin"', detail: 'Still cleartext, but custom string is better than "public". Still not encrypted.',        flag: true },
      { id: 'snmpv3',  label: 'SNMPv3 with auth + privacy (authPriv)',  detail: 'SHA auth + AES encryption. The only secure SNMP version.',                               flag: false },
      { id: 'telnet2', label: 'Telnet enabled for CLI management',      detail: 'All CLI commands including "enable" passwords sent in plaintext. Replace with SSH.',      flag: true },
      { id: 'ssh2',    label: 'SSHv2 for CLI management',               detail: 'Encrypted CLI session. Current best practice.',                                           flag: false },
      { id: 'tftp2',   label: 'TFTP for firmware updates',             detail: 'No authentication, no encryption. An attacker can replace firmware. Use SCP or SFTP.',    flag: true },
      { id: 'ntp2',    label: 'NTPv3 authenticated time sync',          detail: 'Authenticated NTP prevents time-manipulation attacks on Kerberos/logs.',                  flag: false },
    ],
    explanation: 'SNMPv1 and v2c both use cleartext community strings — interceptable with any sniffer. "public" and "private" are default strings; always change them and upgrade to SNMPv3 (authPriv). Telnet management of network devices is indefensible — use SSHv2 exclusively. TFTP has no authentication, enabling firmware replacement attacks — use SCP. SNMPv3 (authPriv), SSHv2, and authenticated NTP are the secure alternatives.',
  },
  {
    id: 'ht-04',
    kind: 'click-multi',
    prompt: 'A security review of a Windows 10 workstation in a financial firm reveals the following settings. Identify ALL hardening failures.',
    instruction: 'Select every item that violates security hardening best practices.',
    items: [
      { id: 'auto-run',  label: 'AutoRun enabled for USB drives',           detail: 'AutoRun executes code from USB automatically — a malware delivery vector. Disable.',   flag: true },
      { id: 'uac-off',   label: 'User Account Control (UAC) disabled',      detail: 'UAC prevents privilege escalation. Disabling it removes a critical defense layer.',    flag: true },
      { id: 'bitlocker', label: 'BitLocker full-disk encryption enabled',   detail: 'Protects data at rest if device is stolen. Correct hardening practice.',               flag: false },
      { id: 'guest-on',  label: 'Guest account enabled with no password',   detail: 'Guest accounts provide unauthenticated access. Disable or set strong password.',       flag: true },
      { id: 'wfas',      label: 'Windows Firewall with Advanced Security ON', detail: 'Host-based firewall. Best practice to leave enabled.',                               flag: false },
      { id: 'macro',     label: 'Office macros: all macros enabled',        detail: 'Macros are a top malware delivery method. Should be disabled or restricted to signed.', flag: true },
      { id: 'screensave', label: 'Screen lock after 5 min of inactivity',   detail: 'Locks unattended workstations. Financial sector typically requires ≤5 min.',           flag: false },
    ],
    explanation: 'AutoRun is a classic USB malware vector — disable it via Group Policy. UAC is a critical access control barrier; disabling it allows malware and users to silently elevate privileges. Enabled guest accounts with no passwords give any physical visitor unauthenticated local access. Unrestricted Office macros are the #1 malware delivery mechanism in phishing attacks — restrict to digitally signed macros only. BitLocker, Windows Firewall, and screen lock are all correct hardening controls.',
  },
  {
    id: 'ht-05a',
    kind: 'click-multi',
    prompt: 'A cloud security audit of an AWS environment identifies the following configurations. Flag ALL items that represent security misconfigurations.',
    instruction: 'Select every misconfigured cloud resource or overpermissive setting.',
    items: [
      { id: 'pub-s3',    label: 'S3 bucket "company-backups" — Public Read Access: ENABLED',         detail: 'Customer backup data is publicly readable. Anyone with the URL can access it.',            flag: true },
      { id: 'priv-s3',   label: 'S3 bucket "app-assets" — Public Read: ENABLED (static site)',       detail: 'Intentionally public for static website hosting. Not a misconfiguration.',               flag: false },
      { id: 'root-key',  label: 'AWS root account — Access key created and in use by CI/CD pipeline', detail: 'Root account keys should NEVER be created. Use IAM roles for automation.',              flag: true },
      { id: 'iam-star',  label: 'IAM policy: Action: "*", Resource: "*" (attached to dev role)',      detail: '"Star star" policy grants all actions on all resources — full admin equivalent.',        flag: true },
      { id: 'iam-least', label: 'IAM policy: Action: s3:GetObject, Resource: arn:aws:s3:::reports/*', detail: 'Scoped to specific action and specific S3 prefix. Correctly follows least privilege.',  flag: false },
      { id: 'mfa-root',  label: 'MFA on root account: NOT ENABLED',                                  detail: 'MFA on the root account is non-negotiable. The root account has unlimited privileges.',  flag: true },
      { id: 'sg-rdp',    label: 'Security Group: Inbound RDP (3389) from 0.0.0.0/0 (all internet)',  detail: 'Open RDP to the internet is one of the most exploited cloud misconfigurations.',         flag: true },
      { id: 'sg-https',  label: 'Security Group: Inbound HTTPS (443) from 0.0.0.0/0',               detail: 'Public HTTPS access is expected for a web-facing server. Not a misconfiguration.',      flag: false },
    ],
    explanation: 'Cloud misconfigurations are the leading cause of cloud data breaches. Public S3 buckets with sensitive data expose everything without authentication. Root account access keys violate AWS best practices — use IAM roles/users instead. Wildcard IAM policies ("Action: *") grant unscoped admin access — a massive privilege escalation risk. Root MFA is mandatory (NIST, CIS, AWS Well-Architected). RDP exposed to 0.0.0.0/0 is actively scanned and exploited within minutes of exposure. Intentionally public static assets and scoped IAM policies are correct configurations, not findings.',
  },
  {
    id: 'ht-06',
    kind: 'click-multi',
    prompt: 'An enterprise endpoint security audit reveals the following application control and mobile device management settings. Flag ALL configurations that violate hardening best practices.',
    instruction: 'Select every setting that should be changed or disabled to improve security posture.',
    items: [
      { id: 'no-allowlist',  label: 'Application control: "Allow all applications to run by default" (no allowlisting)', detail: 'Default-allow lets any executable run — including malware. Allowlisting only permits explicitly approved software.', flag: true },
      { id: 'allowlist-on', label: 'Application allowlist: only signed executables from approved vendors', detail: 'Restricts execution to known-good software. Effectively blocks most malware, ransomware, and fileless attack payloads.', flag: false },
      { id: 'no-mdm-enc',   label: 'Mobile devices: encryption NOT enforced via MDM policy',           detail: 'Mobile devices without encryption expose all data if lost or stolen. MDM must enforce full-device encryption.', flag: true },
      { id: 'mdm-wipe',     label: 'Mobile MDM: remote wipe enabled and tested quarterly',             detail: 'Remote wipe capability is essential — allows IT to erase a lost/stolen device instantly.', flag: false },
      { id: 'jailbreak-ok', label: 'MDM policy allows jailbroken/rooted devices to enroll',            detail: 'Jailbroken/rooted devices bypass OS security controls, rendering MDM and containerization ineffective.', flag: true },
      { id: 'bios-pass',    label: 'BIOS/UEFI password NOT set on laptops',                            detail: 'Without a BIOS password, an attacker with physical access can boot from USB to bypass OS controls.', flag: true },
      { id: 'secure-boot',  label: 'Secure Boot enabled — only signed OS bootloaders permitted',       detail: 'Secure Boot prevents bootkit/rootkit infections at firmware level. Should be enabled on all enterprise endpoints.', flag: false },
    ],
    explanation: 'Application allowlisting (default-deny) is the most effective control against malware — NIST and CIS Controls both rank it in the top tier. Default-allow (no allowlisting) lets any executable run. Mobile devices without MDM-enforced encryption are a physical data breach risk. Jailbroken devices bypass containerization (the corporate data/personal data separation MDM provides). BIOS passwords and Secure Boot work together to prevent physical boot attacks. A missing BIOS password combined with no Secure Boot means an attacker with 5 minutes of physical access can extract or modify any data.',
  },
  {
    id: 'ht-07',
    kind: 'click-multi',
    prompt: 'A CISO reviewing a hardening baseline for containerized workloads finds the following container and Kubernetes configurations. Identify ALL security misconfigurations.',
    instruction: 'Flag every container or orchestration setting that violates least-privilege and security hardening principles.',
    items: [
      { id: 'root-cont',  label: 'Container running as root (no user namespace mapping)',                 detail: 'Root in container = root on host if container escapes. Always run containers as non-root with USER directive.', flag: true },
      { id: 'priv-cont',  label: 'Container SecurityContext: privileged: true',                          detail: 'Privileged containers have full host access including all devices and capabilities. Extreme security risk.', flag: true },
      { id: 'ro-fs',      label: 'Container filesystem: readOnlyRootFilesystem: true',                   detail: 'Read-only filesystem prevents attackers from writing malware or modifying binaries even if they get RCE.', flag: false },
      { id: 'net-policy', label: 'Kubernetes NetworkPolicy: no policies defined (all pods can talk to all pods)', detail: 'Without NetworkPolicies, any compromised pod can communicate with all others — no microsegmentation.', flag: true },
      { id: 'default-sa', label: 'Pods using the default service account with cluster-admin binding',   detail: 'Default service accounts are over-privileged by default. Cluster-admin binding means RCE = full cluster takeover.', flag: true },
      { id: 'secrets-env', label: 'Kubernetes Secrets mounted as environment variables in pod spec',    detail: 'Env vars are visible in process listings, debug logs, and crash dumps. Use volume mounts instead.', flag: true },
      { id: 'resource-lim',label: 'Pod resource limits (CPU/memory) defined in all deployments',        detail: 'Resource limits prevent a single compromised pod from consuming all node resources (DoS). Correct practice.', flag: false },
    ],
    explanation: 'Container security follows least-privilege just like OS security. Running as root removes the user-level isolation container namespaces provide. privileged:true gives full host access — almost always wrong. Network policies are required for microsegmentation (Zero Trust in containers). Default service accounts often inherit excessive permissions — create dedicated, scoped accounts per workload. Secrets as environment variables leak into process memory, logs, and child processes — mount them as files instead. Read-only filesystems and resource limits are correct hardening measures.',
  },
  {
    id: 'ht-05',
    kind: 'click-multi',
    prompt: 'A newly provisioned cloud VM has the following configuration. It will host an internal web application accessible only within the corporate VPN. Flag ALL hardening issues.',
    instruction: 'Select every item that should be changed before production deployment.',
    items: [
      { id: 'default-creds', label: 'SSH login: root / password123',          detail: 'Default/weak root credentials. One of the most exploited initial access vectors.',    flag: true },
      { id: 'root-ssh',      label: 'PermitRootLogin yes in sshd_config',     detail: 'Root SSH login should be disabled. Use sudo from a non-root account.',               flag: true },
      { id: 'pass-auth',     label: 'PasswordAuthentication yes (SSH)',        detail: 'Password auth is vulnerable to brute-force. Use key-based auth only.',               flag: true },
      { id: 'key-auth',      label: 'SSH key-based auth configured',           detail: 'Key-based auth is far stronger than passwords. Keep this enabled.',                  flag: false },
      { id: 'all-ports',     label: 'Firewall: 0.0.0.0/0 allow on all ports', detail: 'Completely open inbound firewall. Should restrict to specific ports and source IPs.', flag: true },
      { id: 'unattended',    label: 'Unattended-upgrades (auto security patches) enabled', detail: 'Automatic security patches reduce window of vulnerability exposure.',     flag: false },
      { id: 'ssh-port',      label: 'SSH on port 22 (default)',                detail: 'Changing SSH port is security through obscurity — not a hardening priority.',        flag: false },
    ],
    explanation: 'Default credentials are the #1 initial access vector in cloud compromises. Root SSH login bypasses the principle of least privilege — attackers immediately have full system control. Password auth is brute-forceable; disable it and use key-based auth exclusively. A completely open firewall rule is never acceptable — restrict to required ports and source CIDRs. Auto security patching reduces exposure window. Note: changing SSH from port 22 is security theater — obscurity, not actual security.',
  },
]
